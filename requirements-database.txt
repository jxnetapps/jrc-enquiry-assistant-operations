# Database dependencies for answer storage
# Install with: pip install -r requirements-database.txt

# SQLite (built-in, no additional package needed)

# MySQL support
PyMySQL==1.1.0

# MSSQL support
pyodbc==5.0.1
