# Requirements for Chroma Cloud integration
# Install with: pip install -r requirements-cloud.txt

# Base requirements
-r requirements.txt

# ChromaDB for cloud integration
chromadb==0.4.15

# Alternative: If ChromaDB installation fails on Windows, try:
# pip install chromadb --no-deps
# pip install grpcio onnxruntime kubernetes posthog pulsar-client
